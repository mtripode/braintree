{% block _order_braintree_entrepids_widget %}
    {% if text is not empty %}
        {% set text = text %}
    {% else %}
        {% set text = 'oro.ui.empty'|trans %}
    {% endif %}

    {{ parent_block_widget(block) }}
{% endblock %}